# 🌊 Tourism Dashboard - System Guide

## ⚠️ Инструкции для AI-агента

**ЭТОТ РАЗДЕЛ СОДЕРЖИТ СТРОГИЕ ИНСТРУКЦИИ. ОБЯЗАТЕЛЬНО К СОБЛЮДЕНИЮ.**

### 🔄 Рабочий процесс
1. **Все правки делаются локально** — агент изменяет файлы в рабочей директории `calm-life/tourism/`
2. **Проверка пользователем** — после внесения изменений пользователь проверяет их локально
3. **Git push пользователем** — только пользователь делает `git push` в репозиторий после проверки
4. **Деплой на сервер** — пользователь скачивает обновления с репозитория на свой сервер

**НЕ ДЕЛАЙ `git push` БЕЗ ЯВНОГО ЗАПРОСА ПОЛЬЗОВАТЕЛЯ!**

### 📝 Git Commit
- **ВСЕ** комментарии к `git commit` писать **СТРОГО НА РУССКОМ ЯЗЫКЕ**
- Кратко описывать суть изменений
- Пример: `git commit -m "Добавлена детализация по дням для сотрудников"`

### 🎯 Контекст проекта
- **Технологии:** Flask (Python) + MySQL + Pandas + JavaScript (frontend)
- **База данных:** MySQL, таблица `records` содержит данные по сотрудникам
- **Поля в records:** fio, snils, sp_nsp, podrazdelenie, otdel, dolzhnost, rukovoditel, status_field, data, chasy, stavka_oklad, stavka, nachisleno, itogo
- **Подразделения (podrazdelenie):** "Арт_Лайф" или "Волна" (Sea Village)
- **Статус (status_field):** "Активен" или "Уволен"
- **Роли:** admin, manager, user — с системой прав на просмотр подразделений и отделов
- **Ключевые фичи:** LFL-анализ (сравнение периодов), группировка сотрудников, модалки с детализацией, экспорт в Excel
- **API эндпоинты:** `/api/data`, `/api/lfl`, `/api/fot/summary`, `/api/fot/breakdown`
- **Шаблоны:** `templates/dashboard.html`, `templates/admin.html`
- **CSS:** `static/css/dashboard.css`, `static/css/base.css`
- **Основная логика frontend:** встроена в `templates/dashboard.html` (inline JS)

---

## 📋 О проекте

**Название:** Tourism Dashboard (Волна Sea Village / Art-Life / ФОТ)  
**Тип:** Веб-приложение для учёта ежедневного туризма сотрудников (ФОТ - фонд оплаты труда)  
**Версия:** 2.5 (с ФОТ-аналитикой, новыми API эндпоинтами `/api/fot/summary` и `/api/fot/breakdown`)  
**Последнее обновление:** 2026-05-24 (Инпуты дат на одной линии, разделитель, компактный дизайн)  
**Версия:** 2.5.17  
**Статус:** ✅ Production Ready

---

## 🤖 Автоматизация процесса загрузки данных

### Текущий ручной процесс

```
Google Sheets (16 книг) → Power Query (Excel) → Штатное расписание → Архив → Админ-панель → Дашборд
```

**Шаги:**
1. С 16 книг Google Sheets через Power Query (Excel) собираются данные по табелям сотрудников
2. В Power Query преобразование в строчный вид (Unpivot)
3. В Excel подстановка значений ставок из листа штатного расписания
4. Расчет "Начислено" = Часы × Ставка
5. Копирование с листа "Реестр" на лист "Архив"
6. Заливка изменений через Админ-панель на дашборд

---

### Анализ Power Query скрипта (convert_tabel.pq)

**Операции:**
| Операция | Power Query | Python аналог |
|----------|-------------|---------------|
| Подключение к Google Sheets | `Excel.Workbook(Web.Contents(url))` | `pd.read_excel(url)` |
| Пропуск 2 строк | `Table.Skip(2)` | `df.iloc[2:]` |
| Повышение заголовков | `Table.PromoteHeaders()` | `df.columns = df.iloc[0]` |
| Фильтрация пустых строк | `Table.SelectRows()` | `df[df['Column1'].notna()]` |
| Unpivot (свертывание дат) | `Table.UnpivotOtherColumns()` | `df.melt()` |
| Фильтрация служебных строк | `Table.SelectRows()` | `df[~df['Дата'].str.contains('Column')]` |
| Преобразование типов | `Table.TransformColumnTypes()` | `df.astype({'Дата': 'datetime'})` |
| Переименование | `Table.RenameColumns()` | `df.rename(columns={...})` |

---

### Предложенные варианты автоматизации

#### Вариант 1: Python + Google Sheets API (Рекомендуется ⭐)

**Архитектура:**
```
Google Sheets → Google Sheets API → Python скрипт → MySQL → Дашборд
```

**Компоненты:**
- `tourism_automation.py` — основной скрипт обработки
- `google_credentials.json` — OAuth2 credentials (секретный)
- Планировщик задач (cron / Task Scheduler)

**Плюсы:**
- ✅ Прямой доступ к Google Sheets без Excel
- ✅ Power Query не нужен
- ✅ Полный контроль над логикой обработки
- ✅ Можно добавить валидацию и логи
- ✅ Работает на сервере без GUI
- ✅ Бесплатно

**Минусы:**
- ❌ Нужно настроить OAuth2 credentials
- ❌ Требует разработки дополнительного скрипта

**Файлы для создания:**
```
calm-life/tourism/
├── tourism_automation.py      # Основной скрипт автоматизации
├── google_credentials.json     # OAuth credentials (добавить в .gitignore)
├── requirements.txt            # Зависимости (google-api-python-client, pandas, mysql-connector-python)
└── logs/
    └── automation.log          # Логи выполнения
```

**План реализации:**
1. Создать сервисный аккаунт Google Cloud
2. Дать доступ к Google Sheets (share с сервисным аккаунтом)
3. Написать скрипт обработки (аналог Power Query на pandas)
4. Настроить планировщик задач (ежедневно в 18:00)
5. Добавить логирование и email-алерты при ошибках

---

#### Вариант 2: Google Apps Script + Webhook

**Архитектура:**
```
Google Sheets → Apps Script (триггер) → Webhook (Flask API) → MySQL
```

**Плюсы:**
- ✅ Работает внутри Google Sheets
- ✅ Триггеры на события (при изменении)
- ✅ Не нужен отдельный сервер для скрипта

**Минусы:**
- ❌ Ограничения выполнения (max 6 мин)
- ❌ Сложнее отладка
- ❌ Меньше гибкости в логике

---

#### Вариант 3: Power Automate / Zapier (No-code)

**Архитектура:**
```
Google Sheets → Power Automate → HTTP Request → Flask API → MySQL
```

**Плюсы:**
- ✅ Визуальный конструктор
- ✅ Быстрая настройка
- ✅ Не нужен код

**Минусы:**
- ❌ Платные подписки
- ❌ Ограничения по количеству вызовов
- ❌ Меньше контроля

---

#### Вариант 4: Гибридный (Excel + Python монитор)

**Архитектура:**
```
Google Sheets → Power Query (автоматически) → Excel файл → Python монитор → MySQL
```

**Плюсы:**
- ✅ Сохраняется текущий процесс с Excel
- ✅ Python только мониторит изменения

**Минусы:**
- ❌ Требуется Excel на сервере
- ❌ Мониторинг файлов менее надежен

---

### Вопросы для проработки (TODO)

- [ ] **Структура штатного расписания:** Какие колонки? (СНИЛС, ФИО, Ставка, Должность...)
- [ ] **Формула расчета:** `Начислено = Часы × Ставка`? Коэффициенты СП/НСП?
- [ ] **16 книг:** Все в одинаковом формате? Список URL-ов?
- [ ] **Архив:** Нужно ли накапливать исторические данные?
- [ ] **Google Sheets API:** Готовность настроить сервисный аккаунт?
- [ ] **Выбор варианта:** Какой вариант автоматизации реализуем?

---

### Текущий статус автоматизации

**Статус:** 📋 Планирование (не реализовано)  
**Приоритет:** Средний  
**Следующий шаг:** Обсудить вопросы выше и выбрать вариант

---

## Changelog

### v2.5.17 (2026-05-24) — 📅 Инпуты дат на одной линии
- 📅 **Инпуты дат на одной линии** — Блоки "Текущий период" и "Сравниваемый период" расположены горизонтально
  - `.lfl-custom-inputs`: `flex-wrap: nowrap` для запрета переноса
  - `gap: 40px` для достаточного расстояния между блоками
  - `align-items: flex-end` для выравнивания по нижней границе
  - Добавлен разделитель `—` между датами (`.lfl-date-separator`)
  - Уменьшена ширина инпутов до 140px для компактности
  - Уменьшены padding и font-size для экономии места
  - Визуально сбалансировано над кнопками месяцев/годов
- 📱 **Mobile Responsive** — На мобильных инпуты становятся вертикально
  - `flex-direction: column`, `flex-wrap: wrap`
  - Инпуты на всю ширину с `max-width: 150px`

### v2.5.16 (2026-05-24) — 🎨 Horizontal Date Inputs
- 🎨 **Horizontal Date Inputs** — Инпуты дат в "Свой период" расположены горизонтально
  - `.lfl-custom-inputs`: `flex` с центрированием для симметрии над кнопками
  - `.lfl-input-wrapper`: обёртка для label + инпуты, `align-items: center`
  - `.lfl-date-inputs`: контейнер для двух инпутов (from/to) в ряд
  - Экономия вертикального места, использование горизонтального пространства
  - Визуально сбалансировано над блоками кнопок месяцев/годов
- 📱 **Mobile Responsive** — На мобильных инпуты становятся вертикально
  - `flex-direction: column` для `.lfl-custom-inputs`
  - Инпуты на всю ширину с `max-width: 170px`

### v2.5.15 (2026-05-24) — 📊 Employee Detail Enhancements
- 📅 **Default Filter: Last Month** — По умолчанию в фильтрах установлен период "Предыдущий месяц"
  - `loadData()`: `setCurrentMonth()` → `setLastMonth()`
- 👁️ **Nav Visible on Load** — Блок навигации (◀ ▶) виден сразу при загрузке страницы
  - Убран `style="display:none;"` из `#lflNavButtons`
  - Показывается для режимов "Месяц" и "Неделя" по умолчанию
- 👥 **Employee Breakdown by Position** — В сводке по сотрудникам данные не агрегируются по ФИО
  - Группировка по уникальному ключу: `ФИО + Должность + Подразделение + Отдел`
  - Сотрудник с несколькими должностями отображается отдельными строками
  - `renderEmpTable()`: агрегация по `fio|dolzhnost|podrazdelenie|otdel`
  - Заголовок таблицы: "Отдел" → "Подразделение"
- 📋 **LFL Modal: Position & Rate Columns** — В модалку "Детализация изменений" добавлены столбцы
  - Таблица сотрудников: + "Подразделение", + "Должность", + "Ставка"
  - `aggByEmp()`: группировка по `fio|dolzhnost|podrazdelenie`
  - Сохранение и отображение `stavka` в ставке

### v2.5.14 (2026-05-24) — 🎯 Integrated Navigation Controls
- 🎯 **Integrated Nav Buttons** — Навигация (◀ ▶) перемещена в блок `.lfl-controls`
  - Расположена перед кнопками режимов в одну линию
  - Идентичная высота с кнопками (38px)
  - Гармоничный дизайн: cyan рамка, тёмный фон, hover-эффекты
  - Скрывается в режиме "Свой период", показывается в "Месяц" и "Неделя"
- 📱 **Mobile Responsive** — На мобильных навигация растягивается на всю ширину
  - `order: -1` для размещения на отдельной строке сверху
  - Адаптивные размеры кнопок и label

### v2.5.13 (2026-05-24) — 🎯 Independent Period Selection & UI Polish
- 🎯 **Independent Period Selection** — Выбор периодов теперь полностью независимый
  - `selectLFLMonth()` и `selectLFLYear()` больше не меняют автоматически другой период
  - Можно сравнивать любые периоды: например, Май-Июль 2026 vs Май-Июль 2025
  - Кнопки месяцев и годов работают независимо для "Текущий" и "Сравниваемый" период
- 📍 **Centered Nav Buttons** — Навигация (◀ ▶) отцентрирована над "Текущий период"
  - Добавлен `.lfl-nav-wrapper` для позиционирования
  - `margin-left: calc((100% - 33.33%) / 2)` для центрирования над первой карточкой
- 📝 **Centered Section Headers** — Заголовки "Текущий период" и "Сравниваемый период" по центру
  - `.lfl-custom-period-section` с `align-items: center`
  - `text-align: center` для `h4`
- 🔢 **Years Above Months** — Кнопки годов расположены над кнопками месяцев
  - Логичный порядок: сначала выбираем год, потом месяц
  - `.lfl-years-row` перед `.lfl-months-row` в HTML
  - `.lfl-years-row { margin-bottom: 12px }` для визуального разделения
- 📱 **Mobile Responsive** — Адаптация под мобильные устройства

### v2.5.12 (2026-05-24) — 🎨 LFL UI Improvements
- 🎨 **Moved Month/Year Selectors** — Кнопки быстрого выбора месяцев/годов перенесены из карточек в блок "Свой период"
  - Убраны `.lfl-quick-select` из карточек "Текущий период" и "Предыдущий период"
  - Добавлен `.lfl-custom-quick-select` внутри `#customPeriodSelector`
  - Два независимых раздела: "Текущий период" и "Сравниваемый период"
  - **Подсветка активных кнопок** — выбранные месяц/год подсвечиваются градиентом
- 🎯 **Repositioned Nav Buttons** — Кнопки навигации (◀ ▶) перемещены влево, теперь над "Текущий период"
  - `justify-content: center` → `justify-content: flex-start`
  - `width: fit-content` для компактного отображения
- 🔧 **Updated JS Functions** — `updateQuickSelectorsHighlight()` для подсветки, `renderQuickSelectors()` для custom mode
- 📝 **Updated README** — Добавлена документация изменений

### v2.5.11 (2026-05-24) — 🔧 LFL Navigation Fix
- 🐛 **Fixed LFL Navigation Buttons** — Кнопки навигации теперь корректно обновляют данные
  - Frontend: `calculateLFL()` теперь всегда передаёт периоды из инпутов в API для всех режимов
  - Backend: API `/api/lfl` теперь использует переданные `custom_from/custom_to/prev_from/prev_to` для всех режимов (не только custom)
  - Добавлена функция `initializeLFLPeriods()` для инициализации инпутов при загрузке и переключении режимов
  - Добавлены отладочные логи в функциях навигации

### v2.5.10 (2026-05-24)
- ✨ **LFL Navigation Buttons** — Добавлены кнопки навигации (← →) для переключения периодов в режимах "Месяц к месяцу" и "Неделя к неделе"
- ✨ **Quick Month/Year Selectors** — Быстрый выбор месяцев и годов под каждым блоком периода (текущий и предыдущий независимо)
- 🔧 **Auto-Update Custom Mode** — Убрана кнопка "Apply" в режиме Custom, пересчёт происходит автоматически при изменении дат
- 🔧 **Fixed LFL Mode Switching** — Исправлено переключение режимов LFL, удаление дублирующей функции
- 🐛 **Fixed LFL Empty Animation** — LFL сразу показывает данные при загрузке страницы
- ✨ **LFL Details Modal** — Модальное окно с детализацией изменений по отделам и сотрудникам (клик на блок "Изменение")
- ✨ **Multiple Otdels Selection** — Множественный выбор отделов (служб) для фильтрации
- 🔧 **Fixed LFL Modal Filters** — Модалка учитывает выбранные подразделение и отдел
- 🔧 **Fixed LFL Change Block** — Добавлен hover-эффект, показывающий кликабельность

### v2.5.0 (2026-05-24)
- ✨ **ФОТ Аналитика** — Новые API эндпоинты `/api/fot/summary` и `/api/fot/breakdown`
- ✨ **ФОТ Обзор** — KPI-карточки: сотрудников, часов, начислено, записей
- ✨ **LFL Анализ** — Сравнение периодов (месяц/неделя/пользовательский)
- 🔧 **Improved Permissions UI** — Зелёная подсветка чекбоксов, клик по строке
- 🐛 **Fixed Audit Log Timezone** — Время отображается по MSK (Europe/Moscow)
- 🐛 **Fixed Admin Tab Default** — Вкладка "Обзор" открывается по умолчанию для админа
- 🔧 **Performance** — Убраны CSS-анимации, вызывавшие 100% загрузку CPU